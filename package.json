{
  "name": "hana-desktop-orchestrator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Guarded Windows desktop control platform for HanaAgent.",
  "keywords": ["hanaagent", "desktop", "computer-use", "windows", "automation"],
  "license": "MIT",
  "scripts": {
    "check:package": "node scripts/check-package.js",
    "check:syntax": "node --check routes/widget.js && node --check lib/audit-timeline.js && node --check lib/audit-evidence-export.js && node --check lib/cockpit-summary.js && node --check tools/self-check.js && node --check tools/protocol-test-matrix.js && node --check tools/fixture-sandbox.js && node --check tools/cockpit-summary.js && node --check tools/ui-tree.js && node --check tools/find-control.js && node --check tools/inspect-window.js && node --check scripts/check-package.js && node --check scripts/build-package.js && node --check scripts/smoke-package.js && node --check scripts/install-smoke.js && node --check scripts/final-regression.js",
    "check": "npm run check:syntax && npm run check:package",
    "build:package": "node scripts/build-package.js",
    "smoke:package": "node scripts/smoke-package.js",
    "install-smoke:package": "node scripts/install-smoke.js",
    "final-regression": "node scripts/final-regression.js"
  }
}
